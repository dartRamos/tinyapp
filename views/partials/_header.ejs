<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button
      class="navbar-toggler"
      type="button"
      data-toggle="collapse"
      data-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
    </div>
      <% if (user) { %> <!-- Check if user object is available -->
      <span class="navbar-text mr-3">
        <%= user.email %> <!-- Display user's email or any other info -->
        <form action="logout" method="POST" class="display:inline;">
          <button type="submit" class="btn btn-secondary">Log Out</button>
        </form>
      </span>
    <% } else { %>
    <form action="/login" method="POST" class="d-flex align-items-center">
      <input
        class="form-control"
        type="text"
        name="username"
        placeholder="Username"
        style="width: 300px; margin: 1em"
      />
      <button typer="submit" class="btn btn-success">Login</button>
    </form>
    <% } %>
  </nav>
</header>